{
  "name": "niti-setu-landing-page",
  "version": "1.0.0",
  "description": "Voice-Based Scheme Eligibility Engine - Landing Page",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run client\" \"npm run server\"",
    "client": "cd client && npm run dev",
    "server": "cd server && npm run dev",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "keywords": ["niti-setu", "government", "schemes", "voice", "eligibility"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
